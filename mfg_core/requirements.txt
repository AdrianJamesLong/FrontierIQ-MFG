fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings>=2.6
python-dotenv==1.0.1

# Claude — via Azure AI Foundry (shared/config.py), Anthropic SDK talks to
# both the Foundry endpoint and the direct API with the same client
anthropic>=0.40.0
azure-keyvault-secrets>=4.9

# Microsoft Fabric — SQL analytics endpoint
pyodbc>=5.1.0
azure-identity>=1.19.0
msal>=1.31.0
psutil>=6.0.0
